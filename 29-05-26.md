## Descargar Ubuntu 24.04 en WSL para Powershell
Abrir Powershell como administrador y escribir:
```c 
wsl --install -d Ubuntu-24.04
```

---

## 1. Instalación de Herramientas de Conexión Remota (SSH)
```c
sudo apt-get install ssh
```
Instala el paquete completo de SSH. Incluye el cliente (para conectarte a otros equipos) y el servidor (para recibir conexiones).

```c
sudo apt install openssh-server
```
Instala específicamente el componente de servidor. Permite que tu máquina acepte conexiones remotas de forma segura.

## 2. Gestión del Servicio SSH
```c
sudo systemctl start ssh
```
Enciende el servidor SSH en este momento. Activa la escucha de conexiones entrantes de inmediato.

```c
sudo systemctl enable ssh
```
Configura el servidor SSH para que se inicie automáticamente cada vez que la computadora se encienda.

## 3. Red y Diagnóstico (Net-Tools)
```c
sudo apt install net-tools
```
Instala un conjunto clásico de herramientas de red para Linux. Incluye comandos antiguos pero muy usados como ifconfig, netstat y route.

```c
sudo ifconfig
```
Muestra la configuración de red actual de tu equipo. Te permite ver la dirección IP local (en la línea que dice inet), la máscara de subred y las interfaces activas.

## 4. Escaneo de Seguridad (Nmap)
```c
sudo apt install nmap
```
Instala el escáner de redes Nmap. Sirve para descubrir qué dispositivos están conectados a una red y qué puertos tienen abiertos.

```c
sudo nmap [IP de inet]
```
Analiza tu propia computadora utilizando su dirección IP. Te muestra qué puertos y servicios (como el SSH que acabas de activar) están abiertos y expuestos en la red.

**EN CMD** conectar cliente a servidor con conexion ssh
```
ssh -p [puerto abierto en ssh, en este caso 22] usuario@direccion_ip
```

# 
# 
# 
# 
# 
# 
# 


Para conectar dos computadoras usando WSL y PowerShell, debes configurar el PC 2 como Servidor (el que recibe la conexión) y el PC 1 como Cliente (el que controla).
Aquí tienes la guía paso a paso para lograrlo y acceder a los archivos.
------------------------------
## Paso 1: Configurar el PC 2 (Servidor / El que será controlado)
Debes asegurarte de que el servidor SSH esté corriendo en Windows o dentro de WSL. La forma más estable en Windows es usar el SSH nativo del sistema.

**Abrir powershell en administrador, NO USAR WSL**

  **1. Abre PowerShell como Administrador en el PC 2 y ejecuta:**
  ```c
  Add-WindowsCapability -Online -Name OpenSSH.Server~~~~0.0.1.0
  ```

**SOLUCIONAR ERROR**
```
Add-WindowsCapability : Error de Add-WindowsCapability. Código de error = 0x8024402c
En línea: 1 Carácter: 1
+ Add-WindowsCapability -Online -Name OpenSSH.Server~~~~0.0.1.0
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (:) [Add-WindowsCapability], COMException
    + FullyQualifiedErrorId : Microsoft.Dism.Commands.AddWindowsCapabilityCommand
```
**ESTE COMANDO (`Add-WindowsCapability -Online -Name OpenSSH.Server~~~~0.0.1.0`)NO FUNCIONA, ASÍ QUE SE USARÁ OTRO METODO**
## Método alternativo
https://github.com/powershell/win32-openssh/releases

* Instalar `OpenSSH win64.zip`
* Copiar la carpeta a **Archivos de programa** en Windows C: con el nombre de OpenSSH

```c
PS C:\windows\system32>
cd "C:\Program Files\OpenSSH"
```
PS C:\Program Files\OpenSSH> 
Set-ExecutionPolicy Bypass -Scope Process -Force
```c
PS C:\Program Files\OpenSSH>
.\install-sshd.ps1
```
<<
Created registry key: HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Image File Execution Options\sshd.exe
Created registry value for sshd.exe to enable RedirectionGuard
  [*] C:\Program Files\OpenSSH\moduli
Inheritance is removed from 'C:\Program Files\OpenSSH\moduli'.
'BUILTIN\Usuarios' now has Read access to 'C:\Program Files\OpenSSH\moduli'.
'APPLICATION PACKAGE AUTHORITY\ALL APPLICATION PACKAGES' now has Read access to 'C:\Program Files\OpenSSH\moduli'.
'APPLICATION PACKAGE AUTHORITY\TODOS LOS PAQUETES DE APLICACIÓN RESTRINGIDOS' now has Read access to 'C:\Program Files\OpenSSH\moduli'.
      Repaired permissions

  [*] C:\ProgramData\ssh
      looks good

[SC] SetServiceObjectSecurity CORRECTO
[SC] ChangeServiceConfig2 CORRECTO
[SC] ChangeServiceConfig2 CORRECTO
sshd and ssh-agent services successfully installed
Updated Machine PATH to include OpenSSH directory, restart/re-login required to take effect globally
```c
PS C:\Program Files\OpenSSH> Set-Service sshd -StartupType Automatic
```

```c
PS C:\Program Files\OpenSSH> Set-Service sshd -StartupType Automatic
Set-Service ssh-agent -StartupType Automatic
Start-Service sshd
Start-Service ssh-agent
New-NetFirewallRule -Name 'OpenSSH-Server-In-TCP' -DisplayName 'OpenSSH SSH Server (sshd)' -Enabled True -Direction Inbound -Protocol TCP -Action Allow -LocalPort 22
```

El error Windows System Error 183 significa que la regla del Firewall ya está creada en tu sistema con el nombre interno 'OpenSSH-Server-In-TCP'. Esto es una excelente noticia: confirma que el instalador de OpenSSH o un intento previo ya configuró el puerto de red.
Dado que la regla ya existe, solo debes asegurarte de que esté activa y que los servicios se inicien correctamente. Ejecuta estos comandos en tu consola de PowerShell como Administrador:
## 1. Activar la regla existente
En lugar de crear una regla nueva, usa este comando para habilitar por completo la que ya tienes:
```c
Enable-NetFirewallRule -Name 'OpenSSH-Server-In-TCP'
```
## 2. Verificar el estado del Firewall
Comprueba que el puerto 22 esté abierto ejecutando lo siguiente:
```c
Get-NetFirewallRule -Name 'OpenSSH-Server-In-TCP' | Select-Object Name, Enabled, Action
```
(Debe mostrar Enabled: True y Action: Allow).
## 3. Iniciar el servicio OpenSSH
Si los comandos anteriores de inicio te dieron dudas, asegura el encendido del servidor con esta línea:
```c
Start-Service sshd; Get-Service sshd
```
(El resultado final en la columna Status debe decir Running).

========================================================================

 **2. Inicia el servicio y configúralo para que arranque automático:**
 ```c
 Start-Service sshd
 Set-Service -Name sshd -StartupType 'Automatic'
 ```
 **3. Obtén la dirección IP local de este PC 2 ejecutando:**
 ```c
 ipconfig
 ```
 (Busca la sección "Dirección IPv4", por ejemplo: 192.168.1.15).

------------------------------
## Paso 2: Conectar desde el PC 1 (Cliente) usando WSL o PowerShell
Ve al PC 1. Puedes abrir PowerShell o tu terminal de WSL (Ubuntu), el comando es exactamente el mismo.

**1. Ejecuta el comando de conexión reemplazando con los datos del PC 2:**
  ```c
  ssh usuario_pc2@IP_del_pc2
  ```
  * `usuario_pc2`: Es el nombre de usuario de Windows en el PC 2 (puedes verlo ejecutando `whoami` en el PC 2).
  * `IP_del_pc2`: La IP que anotaste en el paso anterior (ej. `192.168.1.15`).

**ERROR CONEXIÓN CERRADA**

## Comando universal corregido (Para el PC 2)

   1. Abre PowerShell como Administrador en el PC 2.
   2. Copia y pega exactamente este comando (usa *S-1-5-32-544 que corresponde globalmente a "Administradores" y *S-1-5-18 a "SYSTEM"):
   ```c
   icacls "C:\ProgramData\ssh\ssh_host_rsa_key" /inheritance:r /grant "*S-1-5-32-544:F" /grant "*S-1-5-18:F"
   ```
   3. Reinicia inmediatamente el servicio SSH ejecutando:
   ```
   Restart-Service sshd
   ```

*ssh NB-VNT-02\\\matias.hinrichsen@192.168.14.24*

## 1. El formato correcto para usuarios de dominio (En el PC 1)
Cuando usas cuentas de dominio, no basta con poner el nombre del usuario a secas. Debes indicarle explícitamente el dominio a SSH.
Prueba conectarte desde el PC 1 usando uno de estos dos formatos:

* Formato con barra invertida (Debe llevar triple barra en WSL/PowerShell para que no se rompa el comando):
```C
ssh DOMINIO\\\usuario_pc2@IP_del_pc2
```
Ejemplo: `ssh MIEMPRESA\\\juan@192.168.1.15` 
* Formato alternativo (Estilo correo):
```C
ssh usuario_pc2@dominio.local@IP_del_pc2
```
Ejemplo: `ssh juan@miempresa.local@192.168.1.15} 

## 2. Permitir el grupo del dominio en la configuración SSH (En el PC 2)
Por defecto, OpenSSH en Windows solo deja pasar a administradores locales. Tienes que indicarle que acepte a los usuarios de tu dominio corporativo. [3] 

   1. En el PC 2, abre el bloc de notas como Administrador y abre este archivo: C:\ProgramData\ssh\sshd_config
   2. Ve hasta el final del archivo y busca si existen líneas que digan AllowUsers o AllowGroups.
   3. Agrega la siguiente línea al final de todo para autorizar a tu grupo del dominio (reemplaza por tus datos):
   
   AllowGroups "DOMINIO\Usuarios del dominio" Administrators
   
   4. Guarda el archivo y reinicia el servicio desde PowerShell como Administrador:
   
   Restart-Service sshd
   
   [4] 

## 3. Permiso de lectura en Active Directory (Requiere Administrador de TI)
Si tras probar lo anterior sigue cerrándose al instante, es la confirmación de que la computadora (PC 2) no tiene privilegios para leer el objeto de tu usuario en el Servidor de Dominio. [2] 

* Dile al administrador de red de tu empresa que configure en Active Directory que el objeto de la cuenta de la computadora (PC 2) tenga permisos de "Leer todas las propiedades" (Read all properties) sobre tu cuenta de usuario. [2] 

------------------------------
Para ayudarte mejor en el siguiente paso:
Si ejecutas la conexión agregando la opción de diagnóstico detallado ssh -vvv DOMINIO\\\usuario_pc2@IP_del_pc2, ¿cuáles son las últimas 3 líneas de código que arroja la terminal antes de cerrarse? [5] 

[1] [https://superuser.com](https://superuser.com/questions/1696687/error-authenticating-a-domain-user-for-ssh-connection)
[2] [https://serverfault.com](https://serverfault.com/questions/1067665/unable-to-ssh-into-windows-domain-server-using-domain-user)
[3] [https://community.spiceworks.com](https://community.spiceworks.com/t/windows-open-ssh-sftp-setup-error-disabled-because-of-invalid-user/939751)
[4] [https://superuser.com](https://superuser.com/questions/522094/how-do-i-resolve-a-ssh-connection-closed-by-remote-host-due-to-inactivity)
[5] [https://bbs.archlinux.org](https://bbs.archlinux.org/viewtopic.php?id=234233)





    
**2. La primera vez te preguntará si confías en el equipo. Escribe `yes` y presiona Enter.**

**3. Introduce la contraseña del PC 2.**

¡Listo! Tu terminal ahora estará controlando el PC 2.
------------------------------
## Paso 3: Acceder a los directorios del PC 2
Una vez conectado por SSH, ya estás dentro del PC 2. Para moverte por sus carpetas y ver los archivos usa estos comandos básicos:

* Ver dónde estás parado: Escribe `pwd`. Por defecto entrarás en tu carpeta de usuario (`C:\Users\TuUsuario`).
* Listar archivos y carpetas: Escribe `dir` (en PowerShell) o `ls` (si el PC 2 usa SSH de Linux).
* Moverte a otra carpeta (ej. el Escritorio):
```c
cd Desktop
```

* Ir a la raíz del disco C:
```c
cd C:\
```
------------------------------
## ¿Quieres ver los archivos de forma visual? (Bonus)
Si en lugar de usar la terminal en el PC 1 quieres ver las carpetas del PC 2 en el Explorador de Archivos visual, no necesitas SSH.
En el PC 1, abre el Explorador de archivos, haz clic en la barra de direcciones arriba, escribe \\IP_del_pc2\c$ (ejemplo: \\192.168.1.15\c$) y presiona Enter. Te pedirá el usuario y contraseña del PC 2 y verás todo el disco C de forma gráfica.
Para ayudarte a dejar la conexión perfecta, dime:

* ¿El PC 2 es un Windows normal o quieres conectar directamente dentro de la máquina virtual de WSL del PC 2?
* ¿Te ha aparecido algún error de conexión o de firewall al intentar el comando?
* ¿Necesitas copiar archivos de un PC al otro usando la terminal?


